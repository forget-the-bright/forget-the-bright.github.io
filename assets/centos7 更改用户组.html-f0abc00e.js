import{_ as e,W as a,X as s,a2 as n}from"./framework-3a0c4e99.js";const d={},c=n(`<h1 id="centos7-更改用户组" tabindex="-1"><a class="header-anchor" href="#centos7-更改用户组" aria-hidden="true">#</a> centos7 更改用户组</h1><p><code>这里都是root权限下执行，你也可以加上sudo</code></p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cat</span> /etc/group
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>或者使用管道直接选出是否docker</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">cat</span> /etc/group ｜ <span class="token function">grep</span> <span class="token function">docker</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果出现docker就不用添加，有时会出现dockerroot，没做研究，我也添加docker用户组。 第二步添加docker用户组</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">groupadd</span> <span class="token function">docker</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>也可以指定用户组id（修改id就可以，不要和第一个查出来的id重复）</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">groupadd</span> <span class="token parameter variable">-g</span> <span class="token function">id</span> <span class="token function">docker</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>第三步把普通用户添加docker用户组</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>gpasswd <span class="token parameter variable">-a</span> 普通用户名 <span class="token function">docker</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>第四 更新用户组</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>newgrp <span class="token function">docker</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,13),i=[c];function o(r,l){return a(),s("div",null,i)}const p=e(d,[["render",o],["__file","centos7 更改用户组.html.vue"]]);export{p as default};
